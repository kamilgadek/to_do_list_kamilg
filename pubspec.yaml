name: to_do_list_kamilg
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  app_ui:
    path: packages/app_ui
  bloc: ^8.1.4
  cupertino_icons: ^1.0.6
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  google_fonts: ^6.2.1
  local_storage:
    path: packages/local_storage
  tasks_repository:
    path: packages/tasks_repository

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
